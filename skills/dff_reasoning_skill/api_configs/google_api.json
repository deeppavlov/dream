{
    "google_api": {
        "display_name": "Google API",
        "description": "Performs Google search to answer to a wide variety of questions. If handling the user request requires it, always use this tool to determine the current date.",
        "keys": ["GOOGLE_CSE_ID", "GOOGLE_API_KEY", "OPENAI_API_KEY"],
        "needs_approval": "True",
        "approve_once": "True",
        "timeout": 30,
        "input_template": "Just the request in a string format"
    }
}