{
    "google_api": {
        "display_name": "Google API",
        "description": "Google API utilizes Google to respond to a wide variety of questions.",
        "keys": ["GOOGLE_CSE_ID", "GOOGLE_API_KEY", "OPENAI_API_KEY"],
        "needs_approval": "True",
        "timeout": 30,
        "input_template": "Just the request in a string format"
    },
    "generative_lm": {
        "display_name": "Generative LM",
        "description": "This service uses language models to generate responses.",
        "keys": ["OPENAI_API_KEY"],
        "needs_approval": "False",
        "timeout": 30,
        "input_template": "Just the request in a string format"
    },
    "weather_api": {
        "display_name": "Weather API",
        "description": "The Weather API can provide current weather information for a given location.",
        "keys": ["OPENWEATHERMAP_API_KEY"],
        "needs_approval": "False",
        "timeout": 30,
        "input_template": "Just the request in a string format"
    },
    "news_api": {
        "display_name": "News API",
        "description": "The News API can provide trending news.",
        "keys": ["NEWS_API_KEY"],
        "needs_approval": "False",
        "timeout": 30,
        "input_template": "Just the request in a string format"
    },
    "wolframalpha_api": {
        "display_name": "WolframAlpha API",
        "description": "The WolframAlpha API can perform calculations and do math.",
        "keys": ["WOLFRAMALPHA_APP_ID"],
        "needs_approval": "False",
        "timeout": 30,
        "input_template": "Equation that needs to be solved. DON'T RETURN ANY TEXT. Examples: '5 + 2', '7 + 2x = 12 - 3x'"
    }
}